{
  "name": "builder_update",
  "version": "1.0.2",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder build --win -p never",
    "deploy": "electron-builder build --win -p always",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^9.4.0",
    "electron-builder": "^22.1.0"
  },
  "dependencies": {
    "electron-updater": "^4.2.0"
  },
  "repository": {
    "type" : "git",
    "url": "https://github.com/sundarrajan-ziffity/electron-updater-example"
  }
}
